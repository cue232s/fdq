<h1>Home#index</h1>
<p>Find Restaurants</p>

<%= link_to "Local Restaurants", restaurants_path %>


<ul>
	<% @terms.each do |search_term| %>
		
		<li> <%= link_to(search_term.name, {:controller => "restaurants", :action => "index", :tag_name => search_term.name }) %> </li>

		<% end %>

	<%= debug(@terms.size) %>
</ul>
